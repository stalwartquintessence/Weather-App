{
  "name": "weather",
  "version": "1.0.0",
  "description": "trying node",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "start lite-server"
  },
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}